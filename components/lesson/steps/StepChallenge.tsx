import { ChallengeCard } from '@/types/lesson';

interface Props {
  card: ChallengeCard;
  xpReward: number;
  onReadyChange?: (ready: boolean) => void;
}

export function StepChallenge(_props: Props) {
  return null;
}
