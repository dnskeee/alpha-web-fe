import { QuickFireCard } from '@/types/lesson';

interface Props {
  card: QuickFireCard;
  onReadyChange?: (ready: boolean) => void;
}

export function TestQuickFire(_props: Props) {
  return null;
}
