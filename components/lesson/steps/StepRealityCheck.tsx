import { RealityCheckCard } from '@/types/lesson';

interface Props {
  card: RealityCheckCard;
  onReadyChange?: (ready: boolean) => void;
}

export function StepRealityCheck(_props: Props) {
  return null;
}
