import { ChatSimulatorCard } from '@/types/lesson';

interface Props {
  card: ChatSimulatorCard;
  onReadyChange?: (ready: boolean) => void;
}

export function TestChat(_props: Props) {
  return null;
}
